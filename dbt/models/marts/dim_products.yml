version: 2

models:
  - name: dim_products
    description: "Product dimension for Retail360 analytics."
    columns:
      - name: product_id
        description: "Unique identifier for each product."
        tests:
          - unique
          - not_null