version: 2

models:
  - name: dim_customers
    description: "Customer dimension for Retail360 analytics."
    columns:
      - name: customer_id
        description: "Unique identifier for each customer."
        tests:
          - unique
          - not_null